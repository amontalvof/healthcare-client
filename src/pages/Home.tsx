import { Header, SpecialtyMenu } from '@/components';

const Home = () => {
    return (
        <div className="mt-5">
            <Header />
            <SpecialtyMenu />
        </div>
    );
};

export default Home;

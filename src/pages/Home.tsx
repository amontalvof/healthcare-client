import { Header, SpecialtyMenu } from '@/components';

const Home = () => {
    return (
        <div>
            <Header />
            <SpecialtyMenu />
        </div>
    );
};

export default Home;
